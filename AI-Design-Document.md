# AI 设计文档 — 坦克排协同作战

**课程**：AP6416 / CE6127 Game Artificial Intelligence
**场景**：`MainAI.unity`　**Unity**：2022.3.39f1　**日期**：2026-09-25

> 技术报告，非游戏设计文档。全部内容针对 **NPC AI 决策**。
>
> **合规声明**：AI 相关改动仅存在于 `Assets/Scripts/AI/**` 的 `.cs` 文件与注释。`Assets/Prefabs/**`、`Assets/Scenes/**`、`ProjectSettings/**`、`Packages/**` 相对课程基线零改动（`git diff` 可验），包版本与所有既有血量/速度/伤害数值未变。

---

# 1. AI Design Overview

## 1.1 目标：计分规则决定了设计优先级

比赛计分是 AI 的目标函数，不是"打得好看"：

- 玩家在全部 AI 被摧毁前被摧毁 → **AI 得 3 分**
- 玩家每摧毁一台 AI → **玩家得 1 分**
- 时间耗尽且玩家存活 → **AI 得 0 分**

由此得出贯穿全文的优先级：

| 优先级 | 目标 | 依据 |
|---|---|---|
| **1** | **击毁玩家** | 只有击毁才得 3 分；超时得 0 分 |
| **2** | 保全 AI | 每损失一台送对手 1 分，纯负收益 |
| **3** | 自身生存（在 1、2 之后） | 残血拉扯有价值，但不凌驾于击杀 |

**关键推论：这是一场必须主动进攻的比赛。** 龟缩的期望得分是 0，所以整个 AI 是进攻导向的——`MinFireRange = 0`（贴脸也开火，自伤可接受）就是这条优先级的直接体现。

## 1.2 两个核心亮点

本设计的技术重心集中在两处，后续章节将重点展开：

### 亮点一：射击预测（§2.4）

AI 的炮弹是**纯抛体**，飞抵目标需要 0.4–1.3 秒。这意味着"瞄准玩家当前位置"几乎必然打空。我们建了一套完整的**弹道 + 提前量解算**：

- 按固定步长把玩家的运动**一步步向前外推**，得到一条未来的曲线路径
- 解出「炮弹与玩家**同时到达**」的那个点，并对计时误差做**插值求精确零点**
- 按距离反解出能恰好落在该点的**发射力度**

这是命中率的直接来源，也是本轮修复投入最多的地方（§4.3）。

### 亮点二：反遛狗合围（§3.4）

本作业最有效的逃避战术是**绕中央建筑群转圈**。等速追击在这种情况下会被收敛到玩家正后方排成一列，永远打不到。

我们做了一套**遛狗检测 + 环坐标合围**：检测到玩家在绕圈后，全排从「笛卡尔追击」切换到「环坐标封堵」，用**反向绕**与**内切**两个机制恢复收敛。

## 1.3 三种角色

三台 AI 组成"一推手 + 两翼"，角色按**排内下标轮转**（`m_PlatoonIndex % 3`），与出生顺序无关。

| 角色 | 定位 | 差异 |
|---|---|---|
| **Pusher** | 正面压制 | 交战半径 8m，直接逼近 |
| **LeftFlank** | 左侧包夹 | 半径 ×1.25（10m），环绕方向取反 |
| **RightFlank** | 右侧包夹 | 半径同左翼，方向与左翼相反 |

**为什么是"一正两翼"**：三台同速坦克若都走正面，必然收敛到玩家正后方排成一列。分出正面压力点与两个侧向包夹点，可以让玩家的躲避方向**互相冲突**——向左躲撞左翼，向右躲撞右翼。

## 1.4 策略概述

> **用 FSM 管"我在哪个阶段"，用 Utility 管"打谁、站多远"，用排级环坐标管"我们怎么围住他"。**

三条主线：正面压制 → 侧翼封堵 → 反遛狗合围。

---

# 2. AI Architecture & Logic

## 2.1 技术选型：三种允许方法各有分工

作业允许 FSM / BT / Utility 任意组合。我们用了三种，但每一处都有非它不可的理由：

| 技术 | 用在哪 | 为什么是它 |
|---|---|---|
| **FSM** | 单台坦克的阶段切换 | 阶段是**离散、互斥、有明确进入条件**的，且只有 4 个。用 BT 反而增加无谓层级 |
| **Utility AI** | 目标选择、环绕半径 | 这两个是**连续量的择优**（距离越近越好、血越少该退越远）。硬编码阈值会退化成一堆 if-else |
| **排级协同** | 合围角色分配 | 需要**跨坦克的一致决策**，FSM 与 Utility 都在单机内，故单独做一层共享状态 |

未使用 Behavior Tree：本设计的决策没有"多步有状态的任务序列"，模块化需求由状态机 + 共享检测器已经满足。

## 2.2 FSM：状态与转换

```
   Idle ──有目标──→ Chase ──有视线 且 ≤0.8×射程──→ Attack
     ↑                ↑                              │
     │                └─── >0.95×射程 或 丢视线>0.75s ┘
     └── Patrolling ──有目标──┘
```

**两个设计要点：**

**① 迟滞防抖。** 进入 Attack 的门是 `0.8 × MaxFireRange`，退出才用 `0.95 ×`。两条线之间是一条宽达 15% 射程的死区，玩家在边界来回时状态不会反复横跳。丢视线同理——容忍 0.75 秒，而不是一帧看不见就退出。

**② 用"能否开火"而不是"距离够近"作为交战判据。** 旧代码的门是 `距离 ≤ OrbitRadius() × 1.2`（约 9.6m）。问题是玩家躲在建筑后遛狗时**永远进不了这个距离**，整个回合卡在 Chase；而 Chase 的车头朝行进方向，开火的对准检查永远过不了——结果就是"追不上、打不出枪、回合 0 分"。

新判据换成 `有视线 且 ≤ 0.8×射程`，一有真实射击机会就进入交战姿态。**这是命中率提升最直接的一条改动。**

## 2.3 Utility AI：目标选择与环绕半径

**目标选择**：对每个存活的玩家坦克打分，取最高分。当前效用函数是`直线距离`（取最小），因为全场只有一个玩家坦克，它退化为"选最近的活着的目标"。但结构是通用效用框架——`score` 可以叠加视线质量、残血程度、威胁度等项而**不需要改动 FSM**，这正是选用 Utility 的价值。

**环绕半径**：连续量择优的典型例子：

```
OrbitRadius = EngageRadius(8m) × 角色系数（推手 1.0／侧翼 1.25） × 残血系数（HP<35% → 1.4）
```

**残血为何要退**：每损失一台 AI 送玩家 1 分。一台残血坦克继续贴脸，收益是继续输出，代价是几乎必然被摧毁并送分。35% 血线以下改为拉开距离，是**用射击机会换生存**——赌另两台能完成击杀。这是**连续可调**的权衡，而非硬编码的"血量低就逃跑"状态（后者会在血线两侧反复切换）。

## 2.4 亮点一：射击预测

### 2.4.1 问题

炮弹是**纯抛体**（`Shell.prefab` 的 `m_Drag = 0`，已验证），炮管仰角固定 **+10°**，发射高度 **1.7m**。飞抵 20m 外的目标需要约 0.85 秒——玩家在此期间会移动约 10m。**瞄准当前位置必然打空。**

### 2.4.2 力度解算：落点高度决定了初速

给定水平距离 d，解出炮弹在 d 处到达指定高度的初速度：

```
v² = g·d² / (2·cos²θ·(d·tanθ + (1−k)·h))        k = PassHeightFactor
```

这里有一个反直觉但关键的性质：**`PassHeightFactor` 不是"力度旋钮"，而是"落点高度旋钮"。** 系数越接近 1，炮弹越快——多出的初速恰好被"要多爬一截"抵消。实测取 **1.0**（炮弹快 7%），理由见 §4.3。

### 2.4.3 提前量解算：插值出"同时到达"的点

`PredictTargetPoint()` 用一个**固定步长的循环**，把玩家的运动向前外推 1.5 秒（40 步，每步 0.0375s）。每一步只做两件事——**先把速度方向按角速度转一点，再沿速度前进一小段**：

```
pos = 玩家当前位置;   vel = 玩家当前速度
重复 40 次（每步 0.0375s）:
    vel  按玩家角速度旋转 0.0375s      // 玩家在转弯 → 速度方向跟着转
    pos += vel × 0.0375s               // 沿速度方向前进一小段
```

对得到的每一个采样点 `P(t)`，求**带符号的计时误差**：

```
f(t) = rotateTime + flightTime − t
```

- `f > 0` → 炮弹**晚到**（玩家已走过该点）
- `f < 0` → 早到
- `f = 0` → **炮弹与玩家同时到达，这就是该瞄的点**

在 `f` 变号的那一对采样点之间做**线性插值**求精确零点，而不是取最近的采样点。

> **为什么 horizon 是 1.5s**：解出的 `t` 必须覆盖最长飞行时间。最大射程 39.1m 处飞行 1.32s。**缩短它会直接砍掉远距离的射击能力**（取 0.6s 就只能打 10m 以内）。

### 2.4.4 开火门控

`TryFire()` 逐条检查，全通过才开火：有目标 → 冷却结束（0.7s）→ 射程内 → **对准容差** → 有视线 → 发射。

其中"对准容差"是"角度"还是"距离"，是本轮最关键的一处修复（详见 §2.5 与 §4.3）。

### 2.4.5 演进：预测方案换过一代

当前方案不是一次成型的。第一版（commit `b7ca61d`）的思路是**先按当前位置估一个飞行时间，再用「飞行时间 × 玩家速度」得到提前量，重算距离后再估一次**——如此重复 8 次：

```csharp
Vector3 predicted = Target.position;
for (var i = 0; i < 8; ++i)
{
    // Use horizontal distance so the flight time matches the horizontal speed component.
    Vector3 delta = predicted - FireTransform.position;
    delta.y = 0f;
    float horizontal = delta.magnitude;
    float speed = Mathf.Max(LaunchForceForDistance(horizontal), 0.1f) * Mathf.Cos(m_BarrelPitchRad);
    float flightTime = horizontal / Mathf.Max(speed, 1f);
    predicted = Target.position + TargetVelocity * flightTime;
}
```

注意最后一行：**提前量只由 `Target.position + TargetVelocity × flightTime` 给出**——一条直线。

**它有两个硬伤：**

1. **运动模型是直线。** `Target.position + TargetVelocity × flightTime` 只用了线速度，**等于假设玩家不转弯**。玩家一旦转向，预测点就偏出去——而"绕圈/绕障碍"恰好是玩家最常用的动作。
2. **力度用的是"落点 y=0"的公式**（`LaunchForceForDistance`），炮弹落点是**地面**而不是车身高度，飞行时间也偏长。

第二版（commit `e9aca73`）换成现在这套：把玩家的**角速度**也纳入，用固定步长外推出一条**曲线**路径（§2.4.3）；力度同时改用"落点高度可控"的 `MaxForceWithoutOvershoot`。

| | 第一版 `b7ca61d` | 现在 |
|---|---|---|
| 玩家运动模型 | **直线**（只用线速度） | **曲线**（线速度 + 角速度） |
| **选择标准** | **无** —— 直接返回第 8 次迭代的结果 | **最早可命中的时刻** |
| 求解方式 | 固定 8 次重算 | 40 步外推 + **变号插值** |
| 力度公式 | 落点 y=0（打地面） | 落点高度可控（**打车身**） |
| 车头旋转耗时 | 未计入 | 计入 `rotateTime` |
| 主要缺陷 | 玩家一转弯就偏 | 匀速假设（§4.4） |

**第三个区别不在精度上，而在"选择什么"上——这一点比前两个更本质。**

第一版**没有选择标准**：它把同一个估算重算 8 次，然后返回第 8 次的结果——不检查是否收敛，也不判断算出来的时刻**是否真的可命中**。迭代次数是硬编码的，结果好坏全看那 8 步碰巧走到哪。

现在这一版**有明确的选择标准**：在整个 1.5 秒视野里扫描，返回**最早能命中的那个时刻**（`f` 的**第一个**变号点）。

也就是说，它求的不是"某个时刻玩家会在哪"，而是"**从什么时候起我有机会打中他**"。这更符合射击的语义——**能早打就早打**，压缩玩家的反应时间。旧版则可能返回一个很晚的时刻（玩家早已跑远），或者一个根本打不到的点。

> **这次演进的意义**：第一版的失败恰好证明**"玩家会转弯"是本场景必须建模的因素**。而这正是后来反遛狗检测（§3.4）能成立的前提——玩家的运动是**有规律的曲线**，不是随机抖动，所以才可能被"累积转角 + 圆拟合"识别出来。两次看似独立的改动，其实建立在同一个观察上。

## 2.5 开火容差：为什么近距离反而放宽

容差的设计是**分段的**，而且近距离刻意放宽——这是有意为之，不是妥协。

```
d ≤ 8m ：tolerance = AimToleranceDeg(3.5°) × 近距离放宽系数（最多放宽到 6 倍）
d > 8m ：tolerance = atan(AimLateralTolerance(0.5m) / d)      ← 固定横向余量
```

**近距离放宽的三条理由：**

1. **溅射补偿精度**。炮弹爆炸半径 **3.33m**。贴脸时即使没有直接命中，爆炸伤害也能覆盖目标——精度本来就不需要那么高。

2. **近距离的方位角变化极快，精确对准本身就难维持**。玩家 12 m/s 横移，在 8m 处造成 **86°/s** 的方位角变化；车头角速度上限 180°/s，虽然跟得上，但要在这种变化率下维持 1° 级的对准是不现实的。

3. **压制与封锁走位**。既然精确点射不划算，近距离改为**保持火力覆盖**反而更优：持续开火给玩家压力，**并压缩它的走位空间**——玩家为躲弹必须不断改变方向，而这些方向恰好被两翼封堵。**用火力把玩家"赶"进包围圈**，比追求一发命中更符合排级协同的目标。

**远距离必须收紧**，因为横向误差 = `距离 × tan(角度)`，会随距离线性增长。旧的**常数角度**（2.45°）在约 22m 处就超过了坦克碰撞体的有效半径（约 0.9m）——**超过 22m 后容差本身就允许打空**。改为固定横向余量 0.5m 后：

| 距离 | 横向误差（旧） | 横向误差（新） |
|---|---|---|
| 8 m | 0.49 m | 0.49 m（不变） |
| 20 m | 0.86 m | **0.50 m** |
| 30 m | 1.28 m | **0.50 m** |
| 39 m | 1.67 m | **0.50 m** |

> **8m 以内为什么不动**：那里原有的 `8/d` 缩放**本身就是"固定横向余量"的思路**（横向误差一直维持在 0.49–0.64m）。新代码只是把这个**已有思路**延续到了 8m 以上，而非另起炉灶。

---

# 3. Platoon Coordination

## 3.1 角色分配

角色按排内下标轮转，三台坦克始终是"一推手 + 一左翼 + 一右翼"，**与出生顺序无关**，且任何一台被击毁都不会让编队退化成"全推手"。环绕方向（`StrafeSign`）左翼取 −1、其余 +1，保证两翼从**相反方向**绕过玩家。

## 3.2 定位

**接近阶段：横向铺开，而不是排队。** 先算拦截点，再沿**玩家速度方向的垂线**横移。

这里的关键是"以谁为基准"：横向偏移必须锚在**玩家自己的运动方向**上。若以 AI 自己的朝向为基准（旧代码的 ±55° 侧翼偏移即如此），偏移会**跟着自己一起转**，无法真正占住玩家的前方；锚在玩家速度上则是**世界坐标里稳定的**基准，三台坦克共享同一参照系才能铺开。

**交战阶段：环绕。** 返回以玩家当前位置为圆心、`OrbitRadius()` 为半径的圆上一点，角度以 25°/s 推进。**半径天然就是交战距离**，所以这个行为自带"保持距离"。

**友军间隔。** 任何目的地生成后都过一道后处理，把落点推离 6m 内的友军。

> ⚠️ 这一道**只管 AI 之间**，**对玩家没有任何距离约束**。所以 AI 会真的撞到玩家脸上——这是设计使然（拦截和反绕都奔着迎头去），代价是贴脸开火会吃到自己炮弹的爆炸伤害（§1.1 已知取舍）。

## 3.3 攻击与撤退

**攻击**：进入 Attack 后移动由环绕点接管，车头**始终**对准预测点（与 Chase 不同——Chase 只在真有射击机会时才偏头），保证随时能开火。

**撤退**：唯一形式是**半径后撤**（HP < 35% → 半径 ×1.4）。没有"逃离战场"行为——那会同时牺牲击杀机会、并因龟缩而必然拿 0 分，理性上不划算。

## 3.4 亮点二：反遛狗合围

### 问题：等速追击的"排队"吸引子

本作业最有效的逃避战术是**绕中央建筑群转圈**。三台 AI 若各自"朝玩家当前位置前进"，会被收敛到玩家**正后方排成一列**——因为对任何逃跑中的目标，"最短路径"永远指向它的尾巴，而那条尾巴恰好是建筑遮挡的**阴影区**：没视线、打不出枪、回合 0 分。

### 检测：两个独立信号，必须同时成立

**① 累积朝向角（主判据）**

```csharp
s_CumHeading += Mathf.DeltaAngle(s_PrevHeading, heading);   // 每帧连续累加
```

`DeltaAngle` 返回带符号的最小夹角，因此：**绕圈的玩家单调累加**（每圈 +360°），**左右来回蹭的玩家正负抵消趋近 0**。这一条免费区分了"绕圈"和"抖动"。

**② 对最近 3 秒轨迹做最小二乘圆拟合**

取 12 个位置采样，用 **Kasa 代数拟合**求圆心与半径。技巧在于圆方程 `x² + z² = A·x + B·z + C` 是**关于 A、B、C 的线性方程**——解圆变成解一个 3×3 线性方程组，一次行列式运算出结果，**无需迭代、无需初始猜测**。

**为什么不用瞬时转弯中心 `v/ω`**：绕方形建筑时玩家路径是**圆角矩形**而非圆。瞬时转弯中心是"当前正在过哪个弯"的圆心，玩家一过拐角它就会**跳到建筑的另一个角**。最小二乘拟合把整圈 3 秒一起考虑，拐角被平均掉，圆心稳定；而且它对"绕建筑／绕石头／绕空地"一视同仁，**不需要检测障碍物**。

**防误判**：三点共线（在走直线）、半径超出 [5, 45]m、RMS 残差过大（根本不像圆）三者任一即拒收。另有**迟滞**：进入需累计转角 ≥ **100°**，退出只要掉到 **55°** 以下，避免阈值附近抖动时状态疯狂开关。

> **共享实现**：检器是 `static` 类，每帧只算一次，三台坦克读**同一份**圆心。这是必须的——角色分配要比较"谁最落后"，各算各的会乱序。

### 解法：切换到环坐标

用相对圆心 `Pivot` 的**角度**与**半径**描述一切。两个机制恢复收敛：

**机制 A：反向绕** —— 你顺时针，我逆时针。角度差以 `ω_p + ω_i` 闭合。这是**拓扑保证**：闭环上两个反向运动的点必然相遇，**与速度无关**。

**机制 B：内切** —— 同向，但走**更小的半径**。由 `ω = v/R`，两者线速度都是 12 m/s，半径小的角速度大：

| | 半径 | 角速度 |
|---|---|---|
| 玩家 | 15 m | 45.8°/s |
| AI 内切 | 9 m | **76.4°/s** |
| | | **净赚 30.6°/s** |

**怎么二选一——算出来。** 设落后 `δ` 度、`k = R_玩家 / R_内切`，令两种策略耗时相等：

```
δ_switch = 180 · (k−1) / k        k = 1.67 时 →  δ ≈ 72°
```

落后不到 72° 抄内道更快，超过 72° 掉头迎面堵更快。

**一条强制规则**：**落后最多的那台无条件反向，最靠前的无条件内切**。因为三台刚被甩开时 δ 都很小，按 72° 规则**三台都会选内切** → 同向 → 又排成一列。强制规则保证任何时刻至少有一台在反方向。

**决策稳定性**：角色锁定 1.5 秒，切换阈值带 ±15° 死区。

### 一个容易踩的实现坑：一次只发一个短路点

`NavMeshAgent.SetDestination()` 走**最短路径**。若把"环对面 180° 的位置"直接交给它，agent 会自己选最近的那边走——可能正是你不想让它走的方向，整套逻辑就白做了。所以**每次只发前方 50° 的一小步**，每 0.2s 重发一次，**用高频短步强制 agent 按指定方向绕行**。

**退出条件**：一旦有视线且距离 ≤ 14m，立即交回普通交战。否则已经迎头撞上玩家的坦克会继续绕圈**走开**。

---

# 4. Evaluation & Reflection

## 4.1 测试证据

测试方式：在 `MainAI.unity` 中连续进行 3 回合制对局，由 `GameManager` 自动导出 `Excel/*.xlsx`（含 Match / AI 得分 / 玩家得分 / 回合数 / 机器名）。

**2026-09-25 采集，13 局：**

| 指标 | 结果 |
|---|---|
| **玩家得分** | **13 局恒为 0** —— 玩家一台 AI 坦克都没击毁 |
| AI 得分 | 13 局均 > 0 |
| 最强连续段 | 05:08–05:17 连续 6 局，每局均为满分 9 分（差值 9/9/9/9/9/9） |

最强的一组：连续 6 局每局 3 回合全部拿下，即 **18 个回合 AI 全胜、且零损失**。早前几局曾出现只拿 3 或 6 分（有回合因超时而未击毁玩家），说明"3 回合内稳定击毁玩家"是后段才达到的稳定状态。

> ⚠️ **证据的局限**：这 13 局全部采集于 **03:53–05:17**，而本轮的瞄准/容差修复提交于 **05:18:48**。**没有任何一局是修复后版本的数据**，两组数据不可用于对比。要证明本轮修复效果，需重新采集。

## 4.2 优势

1. **抗规避能力强**。对本作业最有效的逃避战术有专门检测与反制，而非靠运气。
2. **检测鲁棒**。双信号互补：圆拟合拦掉之字形，累积转角拦掉大半径缓弯——单独任何一个都会误判。
3. **排级决策一致**。圆心共享、每帧只算一次，三台坦克在同一坐标系下分配角色。
4. **取舍可复算**。72° 阈值、1.25× 侧翼半径、35% 血线均由推导或实验得出，不是魔数。
5. **合规干净**。改动全部在 `Assets/Scripts/AI/**`，prefab／场景／ProjectSettings 相对基线零改动。

## 4.3 本轮修复的两个缺陷

两者都属于**"模型正确但实现有偏"**——弹道公式、飞行时间公式本身都对，偏差出在实现细节。

### 缺陷 1：提前量系统性偏后

旧实现的循环在 `|f| ≤ 0.05s` 时**提前返回**。但 `f` 是**单调递减穿过零点**的，而循环从 t 小到大扫，所以**第一个落进容差带的采样点必然在正侧**——永远返回"炮弹晚到"的点，等效位置偏差 **0.33–0.60m**。

修复：跑满循环、跟踪 `|f|` 最小值，并对变号的相邻两点**插值**求精确零点，偏差降至 ≈0。

> **这个偏置不能靠加密采样解决**——`0.05` 的单位是**秒**，是个地板。实测 40 点 → 0.374m，200 点 → **0.588m（反而更差）**。

### 缺陷 2：容差是"常数角度"

见 §2.5。横向误差随距离线性增长，约 22m 处超过碰撞体有效半径（0.9m），**超过 22m 后容差本身就允许打空**。这是"炮弹落在玩家身后"的直接来源。改为固定横向余量后，39m 处横向误差从 1.67m 降到 0.50m（收紧 3.3 倍）。

### 一个被实测推翻的假设（如实记录）

我们一度推断 `PassHeightFactor` 应从 1.0 降到 0.7：1.0 时炮弹到达点正好在坦克碰撞体**顶面**（碰撞体纵向 0→1.7m，与发射高度相同），垂直余量只剩炮弹胶囊的 0.195m，看起来任何误差都会"从车顶掠过"。

但**我们把容错低估了一个量级**——真正的容错不是 0.195m 的半径，而是**弹道回升到顶面之前能偏短多少**。由于 `tan 10° = 0.176`，每近 1m 只抬高约 0.18m，实测容错约 **1.1m**。而修复后的提前量偏差仅 0.1m，远在容错之内。

于是 0.7 多买的 0.9m 余量**用不上**，却要付**炮弹慢 7%** 的代价。**最终按实测保留 1.0。** 记在这里是因为这条推理链看着严密却是错的——提醒后来者先核对量级再动手。

## 4.4 局限性

按误差量级排序：

- 🔴 **预测假设"玩家保持当前线速度 + 角速度"**。玩家加速、减速或改变转向时预测失准，误差可达**米级**，远超上述 0.1–0.5m 的修正量。**这是命中率的主要剩余不确定性来源。**
- 🟡 **物理撞击与自伤**。拦截与反绕都奔着迎头去，且友军间隔只约束 AI 之间。`MinFireRange = 0` 使贴脸开火会吃到自己炮弹的爆炸伤害（有意的取舍）。
- 🟡 **全知视角**。AI 直读玩家位置与速度，无视野、无遮挡、无距离限制。开火前仍有视线检测，所以不会穿墙射击，但严格说 AI 能"感知"到它看不见的目标。
- 🟢 **`AcquireRange` 是死字段**，全工程无人读取，`SelectTarget()` 无条件选最近目标。
- 🟢 **炮管仰角是 `Awake()` 缓存值**，而炮弹按实时的炮管朝向发射。正常游戏里 `RotateTowards` 每帧压平车体所以两者一致，车体若出现俯仰（撞车、斜坡）就会失配。

## 4.5 反思：这套策略的成立前提

必须承认有一点"胜之不武"。

**本作中玩家坦克只能朝车头方向开火**，瞄准完全依赖旋转车体，因此玩家**无法"朝一个方向移动、同时朝另一个方向射击"**。这让玩家很难在移动中持续输出，也很难做出"横移出掩体射击、再缩回掩体"这类动作。

后果是：**AI 承受的火力压力比一般射击游戏小得多**。所以我们才敢于把击杀放在生存之前——`MinFireRange = 0` 允许贴脸自伤、残血阈值只到 35%、合围时三台一起压上。

**但这个前提并不通用。** 在更合理的游戏设计中——玩家能横移出掩体开火、射击后立刻缩回掩体、或边绕边打——AI 会持续暴露在火力下，那么：

- `MinFireRange = 0` 的贴脸自伤会从"可接受代价"变成**净亏损**
- 残血后撤的阈值需要大幅提高
- 更根本的是，**"三台一起压上"的合围本身会把 AI 集中到玩家的火力范围内**，需要引入交替掩护、分批推进等真正的生存协同

换句话说，**我们这套设计是针对"玩家机动与射击强耦合"这一特定约束优化出来的**。约束一旦松动，优先级排序（击杀 ≫ 生存）就需要重估。这也解释了为什么本设计**没有把 AI 生存当作一等目标**——不是疏忽，是在给定规则下的合理取舍。

## 4.6 改进方向

1. **改掉匀速假设**（收益最高）。可选：对玩家转向做保守处理（宁可少打一发也不打偏）、缩短有效预测时间、或引入转向时的误差上界估计。
2. **有限感知**。在 `SelectTarget()` 加感知门控，玩家进入视野/范围才允许获取位置，替代全知视角。作业未禁止全知，但显式建模感知在 AI 质量上是加分项。
3. **生存协同**（若前提松动）。交替掩护、分批推进、残血主动脱离。
4. **炮管仰角实时化**，消除车体俯仰导致的模型失配。
5. **清理死字段** `AcquireRange`。
6. **补采修复后的测试数据**（必要），现有数据均早于修复提交。

---

# 附录 A：代码改动清单

| 文件（`Assets/Scripts/AI/`） | 状态 | 说明 |
|---|---|---|
| `Tank/StateMachine/TankSM.cs` | 修改 | 状态机主体、效用打分、弹道解算、开火门控、合围方法 |
| `Tank/StateMachine/OrbitTracker.cs` | **新增** | 遛狗检测（累积转角 + Kasa 圆拟合） |
| `Tank/StateMachine/States/ChaseState.cs` | **新增** | 接近／铺开 |
| `Tank/StateMachine/States/AttackState.cs` | **新增** | 交战环绕 |
| `Tank/StateMachine/States/IdleState.cs` | 修改 | 朝向目标并立即决策 |
| `Tank/StateMachine/States/PatrollingState.cs` | 修改 | 巡逻搜索 |

**未改动**：`Assets/Prefabs/**`、`Assets/Scenes/**`、`ProjectSettings/**`、`Packages/**`。

# 附录 B：关键参数

| 参数 | 值 |
|---|---|
| 炮管仰角 / 发射高度 | +10° / 1.7m |
| 坦克碰撞体纵向范围 | 0 → 1.7m（顶面 = 发射高度） |
| 发射力度范围 / 冷却 | 7.5–30 / 0.7s |
| 最大射程 | ≈ 39.1m（飞行 1.32s） |
| 寻路速度 / 角速度 | 12 m/s / 180°/s |
| 遛狗进入 / 退出阈值 | 100° / 55°（累计转角，迟滞） |
| 圆拟合采样 | 12 点 / 3 秒 |
| 内切切换阈值 | 180(k−1)/k，k=1.67 → **72°** |
| 环路点步长 / 交回距离 | 50° / 14m |
| 角色决策保持 / 死区 | 1.5s / ±15° |
| 交战半径 | 8m（推手）/ 10m（侧翼） |
| 残血后撤 | HP<35% → 半径 ×1.4 |
| 对准容差 | ≤8m：3.5°；>8m：横向 0.5m |
| 落点高度系数 | 1.0 |

# 附录 C：支持性文档

- [`射击流程.md`](射击流程.md) — 开火链路逐方法说明，含弹道推导
- [`codex-apply-anti-kiting.md`](codex-apply-anti-kiting.md) — 反遛狗合围的设计规格与实现任务书
