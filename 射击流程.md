# 射击流程（AI 坦克开火全流程）

本文梳理 AI 坦克从「发现目标」到「炮弹命中」的完整射击链路，供调试命中率时对照，不用再往回翻代码。

> 核心代码都在 [TankSM.cs](Assets/Scripts/AI/Tank/StateMachine/TankSM.cs)，namespace `CE6127.Tanks.AI`。

---

## 0. 一句话概览

AI 坦克每一帧：
1. **估目标速度 + 角速度** → 2. **采样未来 1.5s 曲线路径、插值出「准时命中」的点** → 3. **算到达该点的力度** → 4. **判断是否开火**（冷却 / 射程 / 容差 / 视线） → 5. **发射炮弹**（纯抛体） → 6. **炮弹落地爆炸**（范围伤害）。

---

## 1. 物理 / 固定参数（不可改）

这些是 prefab / 项目里写死的，改动即违规：

| 参数 | 值 | 来源 |
|---|---|---|
| 炮管仰角 θ | **+10°（炮口朝上）** | `FireTransform` 的 `m_LocalRotation` = (0.08716, 0, 0, −0.99619)（模长归一，等价于绕 X 轴 −10°）→ 实际 `forward.y = 0.17365 = sin(10°)` |
| 发射高度 h | **1.7 m** | `FireTransform` 相对坦克原点 y=1.7 |
| **坦克碰撞体纵向范围** | **0 → 1.7 m** | `Tank.prefab` 的 BoxCollider `m_Size={1.5, 1.7, 1.6}`、`m_Center={0, 0.85, 0}`。**顶面高度恰好等于发射高度** —— 这是所有「飞过头顶」问题的根源，见 §7 |
| 炮弹碰撞体 | 胶囊 r=0.15，h=0.55，轴向 Z | `Shell.prefab` CapsuleCollider `m_Direction=2`，随 `FireTransform.rotation` 生成 → 长轴沿飞行方向，**纵向半高 ≈ 0.195 m** |
| 重力 g | **9.81 m/s²** | `DynamicsManager.m_Gravity.y = -9.81` |
| 炮弹空气阻力 | **0**（纯弹道） | `Shell.prefab` Rigidbody `m_Drag=0` |
| 发射力度范围 | **prefab (7.5, 30)**（代码默认 6.5，被 prefab 覆盖） | `LaunchForceMinMax` |
| 射击冷却 | **(0.7, 2.5)**，实际用 **x=0.7**（最快 0.7s/发） | `FireInterval` |
| 炮弹最大寿命 | **1.75 s** | `ShellExplosion.MaxLifeTime` |
| 爆炸伤害 | **MaxDamage=10** | `ShellExplosion` |
| 爆炸半径 | **3.33 m** | `ShellExplosion.ExplosionRadius` |
| 爆炸推力 | **750** | `ShellExplosion.ExplosionForce` |

---

## 2. 可调 AI 决策参数（非物理值，可改）

这些是决策阈值，属于「AI 决策逻辑」范畴：

| 字段 | 默认值 | 作用 |
|---|---|---|
| `MinFireRange` | 0 | 最小开火距离；0 = 贴脸也开火（击杀优先，自伤可接受） |
| `AimToleranceDeg` | 3.5 | **8m 以内**的车头对准角度容差（越小越准） |
| `AimLateralTolerance` | 0.5 | **8m 以上**允许的横向脱靶距离（米），按距离换算成角度（见 §6） |
| `EngageRadius` | 8 | 交战基准距离（决定绕圈半径） |
| `AcquireRange` | 40 | ⚠️ **死字段 —— 全工程无人读取**。`SelectTarget()` 无条件选最近的活着的玩家坦克，没有距离门控；CLAUDE.md 仍把它列为可调项，但调它没有任何效果。要用需先接上，或删掉 |

---

## 3. 弹道模型（物理基础）

炮弹是**纯抛体**（无空气阻力），发射初速度方向 = 炮管朝向（仰角 θ=10°，随车体旋转）：

```
水平: x(t) = v·cosθ·t
竖直: y(t) = h + v·sinθ·t − ½·g·t²
```

落地时 `y(t)=0`，代入 `t = d / (v·cosθ)`（d 为水平距离）得**反解力度公式**：

```
v² = g·d² / ( 2·cos²θ·(h + d·tanθ) )
```

这就是 [LaunchForceForDistance()](#42-launchforcefordistance) 的数学来源。

---

## 4. 完整射击流程（方法调用链）

### 4.1 `TickTargetTracking()` — 估目标速度 + 角速度

每一帧用**前后两帧差分**估算玩家的**线速度**（位置差分）和**角速度**（朝向差分），再各自做低通平滑：

```csharp
// 帧率无关的平滑因子，推导见下
smoothing = 1 - (1 - VelocitySmoothingFactor) ^ (deltaTime × VelocitySmoothingReferenceFps)

instant = (Target.position - prevPosition) / max(deltaTime, 1e-4)
TargetVelocity = Lerp(TargetVelocity, instant, smoothing)

forward = Target.forward 的水平分量（归一化）
angleDeg = SignedAngle(prevForward, forward, up)          // 带符号转角
TargetAngularVelocity = clamp(Lerp(TargetAngularVelocity, angleDeg/dt, smoothing), -180, 180)
```

#### 平滑因子为什么写成这个形式（帧率无关）

原写法是 `Lerp(..., 0.4f)` —— **每帧**前进 40%。这只在帧率固定时等价于一个固定的**时间常数**，而帧率一变，时间常数就跟着变：

| 帧率 | 时间常数 |
|---|---|
| 200 fps | ~10 ms |
| 60 fps | 32.6 ms |
| 30 fps | ~65 ms |

同一份代码，低帧率下 AI 拿到的是**滞后 65 毫秒**的速度估计。而**提前量 = 速度 × 飞行时间** —— 速度估计滞后，提前量就直接算错。

**实测证实了这一点**：standalone build 明显比编辑器弱，而把 build 窗口缩小（帧率升高）就能恢复。说明差异来自帧率，不是别的原因。

现在的写法用 `deltaTime` 做指数补偿。指数里 `deltaTime × 参考帧率` 的含义是「**这一帧相当于几个参考帧率的帧**」：

| 实际帧率 | dt × 240 | 每帧前进量 k | 等效时间常数 |
|---|---|---|---|
| 240 | 1 | 0.4000 | 8.2 ms |
| 120 | 2 | 0.6400 | 8.2 ms |
| **60** | **4** | **0.8704** | **8.2 ms** |
| 30 | 8 | 0.9832 | 8.2 ms |

**所有帧率的等效时间常数都是 8.2 ms** —— 这个才是不变量。

> ⚠️ **`VelocitySmoothingReferenceFps` 只是一个刻度，不是目标帧率。** 它说的是"平滑速度应当等同于该帧率下的逐帧行为"；**游戏并不跑在 240 帧**。物理上有意义的数字是 τ = 8.2 ms。
>
> **取值依据**：240（τ = 8.2 ms）由实测确定 —— 平滑越少越准，说明在这个场景里**滞后比噪声更伤**。

> - 用途：线速度 + 角速度一起给 §4.4 的**曲线路径采样**提供玩家运动模型。
> - 角速度是**观测估计**（差分玩家朝向），不读玩家输入。玩家 `AngularSpeed=180` 是上限，故 clamp ±180。
> - 全知视角（无视野/遮挡限制），可直接读玩家位置/朝向。

#### ⚠️ 已知未解决：差分法的采样混叠

差分法本身还有一个**尚未处理**的缺陷，四条事实叠在一起：

| 事实 | 来源 |
|---|---|
| 物理步长 50 Hz（每 0.02 s 推进一次） | `ProjectSettings` Fixed Timestep = 0.02 |
| 玩家在 `FixedUpdate` 里用 `MovePosition` 移动 | `TankMovement.Move()` |
| 位置**不做渲染插值** | `Tank.prefab` Rigidbody `m_Interpolate: 0` |
| AI 在 `Update()`（渲染帧率）里读位置 | `TickTargetTracking()` |

**50 与 60 除不尽**，所以每一帧看到的位移是「**整数个物理步长**」——可能 1 步，也可能 0 步。而代码用**渲染帧时间**当分母，**单位不匹配**：

```
0.24 m ÷ 0.0167 s = 14.4 m/s     ← 真值 12；分子是"每物理步长"，分母是"每渲染帧"
0.24 m ÷ 0.02   s = 12   m/s     ← 正确的分母
```

所以原始速度估计会随物理频率与渲染帧率之比漂移（偏大、或整帧归零），**当前的平滑滤波实际上是在给这个坏输入打补丁**。

**修法（尚未实施）**：只在物理步长真正推进时取样（用 `Time.fixedTime` 变化做门控），并除以 `Time.fixedDeltaTime`。这样每个采样值都精确，**滤波器届时可以整个删掉**。

### 4.2 `MaxForceWithoutOvershoot(horizontalDistance)` — 按「落点高度」反解力度

给定水平距离 d，反解出炮弹在 d 处**到达 `PassHeightFactor` × 发射高 h** 的初速度（当前 `PassHeightFactor = 1.0`：炮弹跨过目标竖平面时恰好回到 1.7m），clamp 到 `[7.5, 30]`：

```csharp
drop = (1 − PassHeightFactor) · h
v    = clamp( sqrt( g·d² / (2·cos²θ·(d·tanθ + drop)) ), 7.5, 30 )
```

> 推导：令 `y(d) = PassHeightFactor·h`（发射高 h 是炮弹在发射点的高度，也即「车顶」参照）：
>
> ```
> y(d) = h + d·tanθ − ½·g·d²/(v²·cos²θ) = PassHeightFactor·h
>   ⇒  d·tanθ + (1−PassHeightFactor)·h = ½·g·d²/(v²·cos²θ)
>   ⇒  v² = g·d² / (2·cos²θ·(d·tanθ + drop))
> ```
>
> 系数两端对照：`PassHeightFactor = 1` → `v² = g·d/sin(2θ)`，炮弹回到炮口高度；`= 0` → 落地 y=0（即旧 `LaunchForceForDistance`，代码里仍保留但开火已不再使用）。
>
> **当前取 1.0。** 有一个反直觉但重要的性质：**这个系数越接近 1，炮弹越快。**
>
> | `PassHeightFactor` | d=20m 初速 | 飞行时间 | 到达高度 |
> |---|---|---|---|
> | **1.0（当前）** | **23.95 m/s** | **0.848 s** | 1.70 m |
> | 0.7 | 22.39 m/s | 0.907 s | 1.19 m |
>
> 因为解算器保证炮弹在瞄准点到达 `k·h` 这个**高度**：把 k 抬高，多出来的初速**恰好被「要多爬一截」消耗掉**，射程反而略减（31.38m → 30.28m）。**所以 `PassHeightFactor` 不是「力度旋钮」，是「落点高度旋钮」。**
>
> **1.0 与 0.7 的取舍（实测结论，2026-09-25）**：1.0 的炮弹快 **7%**，玩家更难反应；代价是到达点正好在坦克碰撞体的**顶面**（碰撞体纵向 0→1.7m，见 §6），垂直余量只剩炮弹胶囊的 **0.195m**。0.7 把到达点压到车身中部、余量 0.66m，但慢 7%。
>
> **实测 1.0 更好** —— 因为 §4.4 修掉提前量偏置后，剩余脱靶量（~0.1m）远在 1.0 的容错（~1.1m）之内，0.7 多买的那点垂直余量用不上，白付 7% 的速度。commit `0c2b9ad` 曾改成 0.7（理由 "so shots don't fly over"），那是在 §4.4 修复**之前**观察到的现象；修复后已不再必要。

### 4.3 `ComputeMaxFireRange()` — 最大射程

把 `v = 30`（最大力度）代回公式，解一元二次方程求 d 的最大值：

```
g·d² − 2·v²·cosθ·sinθ·d − 2·v²·cos²θ·h = 0
```

→ **最大射程 ≈ 39.1 m**，对应飞行时间 ≈ **1.32 s**（见 §5）。

### 4.4 `PredictTargetPoint()` — 采样未来曲线路径、插值出准时命中点

用玩家**当前速度 + 角速度**离散积分出未来 **1.5s** 的**曲线**路径（**40 个采样点**，步长 0.0375s），对每个采样点 `P(t)` 算**带符号的计时误差** `f`，再在 `f` 变号的那一对采样点之间**插值**求出精确零点：

```csharp
pos = Target.position;  vel = TargetVelocity
for t in 0.0375 .. 1.5 step 0.0375:      // 积分玩家曲线路径
    vel = RotateY(vel, TargetAngularVelocity * step)   // 速度随转弯旋转
    pos += vel * step

    d          = (pos - FireTransform.position).水平距离
    rotateTime = angle(车头, pos方向) / 180   // 车头转到该点耗时
    flightTime = FlightTime(d)                // 以该点为落点的飞行耗时
    f          = rotateTime + flightTime - t  // 带符号

    if f 与上一采样点变号:
        return Lerp(prevPos, pos, prevF/(prevF-f))   // 插值出精确零点
return |f| 最小的那个 pos                     // 兜底：1.5s 内无法拦截
```

> **`f` 的含义**：`f > 0` = 炮弹**晚到**（玩家已走过该点）；`f < 0` = 早到。零点即「炮弹与玩家同时到达该点」，也就是真正该瞄的位置。
>
> **为什么必须带符号 + 插值**（2026-09-25 修复，commit `31c913b`）：
>
> 旧代码取 `diff = |f|`（丢掉了符号），且一旦 `diff ≤ 0.05` 就 `return`。因为 **`f` 是单调递减穿过零点的**，而循环从 t 小到大扫，所以**第一个落进 ±0.05 容差带的采样点必然在正侧** —— 也就是永远返回一个「炮弹晚到」的点，`f` 恒为正、**系统性偏后**。
>
> | 返回点 | 晚到量 | 等效位置偏差（玩家 12 m/s） |
> |---|---|---|
> | 旧：容忍 0.05s 提前返回 | 0.027–0.05 s | **0.33 – 0.60 m** |
> | 新：`\|f\|` 最小 + 插值 | → 0 | **≈ 0** |
>
> 而且这个偏置**不能靠加密采样解决** —— `0.05` 的单位是**秒**，是个地板，采样越密返回点越贴近 0.05s 这个上界（实测：40 点 → 0.374m，200 点 → 0.588m，**反而更差**）。必须先把提前返回去掉。
>
> **`horizon = 1.5s` 的来历**：解出的 `t` 必须覆盖最长飞行时间。最大射程 39.1m 处飞行 **1.32s**（§5），所以取 1.5s 留余量。**缩短它会直接砍掉远距离的射击能力**（取 0.6s 就只能打 10m 以内的目标，超出则循环不收敛）。

### 4.5 `TryFire()` — 开火判定（核心门控）

逐条检查，**全通过才开火**：

1. **有目标**（`HasTarget()`）。
2. **冷却结束**（`Time.time >= m_NextFireTime`）。
3. **算瞄准点**：`aim = PredictTargetPoint() - FireTransform.position`（忽略 y），得 `aimDistance`。
4. **射程内**：`MinFireRange ≤ aimDistance ≤ MaxFireRange`（≈ 39.1m）。
5. **角度容差**：车头方向与瞄准点夹角 ≤ `tolerance`（见 §6）。
6. **有视线**（`HasLineOfSightToTarget()`，§4.6）。
7. **发射**：`LaunchProjectile( MaxForceWithoutOvershoot(aimDistance) )`，冷却重置为 `FireInterval.x = 0.7s`。

### 4.6 `HasLineOfSight()` / `HasLineOfSightToTarget()` — 视线检测

从炮口 `Linecast` 到目标点（目标中心上移 0.85m），若撞到的是坦克（`TankHealth`）则视为可见，撞到墙则不可见：

```csharp
HasLineOfSightToTarget() => HasLineOfSight(Target.position + up·0.85)
```

### 4.7 `LaunchProjectile(launchForce)` — 发射

在 `FireTransform` 处实例化炮弹，赋予初速度（方向 = 炮管朝向）：

```csharp
shellInstance.velocity = launchForce · FireTransform.forward
```

### 4.8 命中 / 爆炸（`ShellExplosion.OnTriggerEnter`）

炮弹飞行 `MaxLifeTime=1.75s` 或撞到物体后，`OverlapSphere(ExplosionRadius=3.33)` 判伤害，按离爆炸中心距离衰减（中心满伤 `MaxDamage=10`）。

---

## 5. 派生数值（射程 / 飞行时间）

用 θ=10°、h=1.7m、g=9.81、drag=0 代入：

| 发射力度 | 水平射程 | 飞行时间 |
|---|---|---|
| 最小 7.5 | ≈ 5.44 m | ≈ 0.74 s |
| 最大 30 | ≈ 39.1 m | ≈ 1.32 s |

> - 所以**一发炮弹要么命中、要么落地，最多 ~1.32s 内**结束（远低于 MaxLifeTime 1.75s，不会因超时自爆）。
> - **贴脸距离（< 5.44m）**：力度被 clamp 到最小值 7.5，理论上会「过射」，但炮弹有体积 + 爆炸半径 3.33m，角度差一点也能命中 → 这就是 `MinFireRange=0` 仍开火能命中的原因。

---

## 6. 开火角度容差如何随距离变化

`TryFire()` 里的容差**不是常数角度**，**8m 是分界**：

```csharp
const float NearRange = 8f;
float rangeFactor = clamp(NearRange / d, 1, 6);
float closeFactor = Lerp(1.3, 1, InverseLerp(5, NearRange, d));

tolerance = (d <= NearRange)
    ? AimToleranceDeg * rangeFactor * closeFactor        // 近距离：角度容差
    : Atan2(AimLateralTolerance, d) * Rad2Deg;            // 远距离：固定横向余量
```

| 距离 | 横向误差（旧） | 容差（新） | 横向误差（新） |
|---|---|---|---|
| 5 m | 0.639 m | 7.28° | 0.639 m |
| 8 m | 0.489 m | 3.50° | 0.489 m |
| 20 m | 0.856 m | 1.43° | **0.500 m** |
| 30 m | 1.284 m | 0.95° | **0.500 m** |
| 39 m | 1.669 m | 0.73° | **0.500 m** |

> **为什么必须改**（2026-09-25，commit `31c913b`）：横向误差 = `距离 × tan(角度)`。旧的**常数角度** ~2.45°（d>14m）让横向误差随距离**线性增长**，在约 **22m** 处超过坦克碰撞体的有效半径（碰撞体 1.5×1.6 + 炮弹半径 0.15 ≈ **0.9m**）—— 也就是说**超过 22m 后，容差本身就允许打空**。这正是「炮弹落在玩家身后」的直接来源。
>
> 又因为 `RotateTowards` 每帧最多转 **3°**（180°/s @60fps），而容差是 **2.45°** —— **不到一帧的转动量**，所以 AI 一进容差带就开火（`> tolerance` 一旦不成立即放行），**实际开火时的角度误差约等于容差上限**，而不是远小于它。
>
> **注意炮弹沿车头方向飞**：预测模型里的 `rotateTime` 补偿的是**时间**（把瞄准点在时间上推远），但炮弹是沿 `FireTransform.forward` 即**车头当时的实际朝向**发射的，那个角度误差**没有任何东西补偿**。容差就是唯一在管它的东西。
>
> 旧代码的 `precisionFactor`（14m 外收紧到 0.7×）本想解决这件事，但误差随距离**线性**增长，要抵消它必须按 **`1/d`** 收 —— 0.7 远远不够。**现已删除**，由 `Atan2` 取代。
>
> **8m 以内为什么不动**：那里 `rangeFactor = 8/d` **本身就是「固定横向余量」的思路**（横向误差一直维持在 0.49–0.64m），而且更紧的角度只会减少开火机会 —— 近距离横向误差本来就在碰撞体内。**新代码只是把这个已有的思路延续到了 8m 以上**，不是新发明。
>
> **代价**：容差收紧 = 车头要等更准才开火，**远距离开火频率会下降**。若实测发现 AI 远距离「不怎么开枪」，把 `AimLateralTolerance` 从 0.5 提到 0.7 即可缓解（0.7m 仍在 0.9m 碰撞体内）。

---

## 7. 命中率相关的已知要点（调试备忘）

> **按误差量级排序，从大到小。**

- 🔴 **最大的剩余误差源是「匀速假设」**。§4.4 的曲线建模假设玩家保持**当前的线速度 + 角速度**。玩家**加速、减速、或改变转向**时预测就失准，误差可达**米级**，远超下面那些 0.1–0.5m 的修正量。**这是目前命中率的主要不确定性来源**；若要继续提升，应从里下手（缩短有效预测时间、或对转向做保守处理），而不是再抠弹道参数。
- 🔴 **速度差分有采样混叠**（详见 §4.1 末）：玩家位置按 **50 Hz 物理步长**跳变，AI 按**渲染帧率**读取，而代码用渲染帧时间当分母 —— 分子分母**单位不匹配**，原始速度估计会偏大或归零。**这是当前最大的未解决误差源**，滤波只是在给它打补丁。
- 🟡 **平滑滤波的滞后**：一阶低通，时间常数由 `VelocitySmoothingReferenceFps` 决定（当前 τ = 8.2 ms），**已与帧率无关**。匀速时无稳态偏差；玩家速度突变后约 8 ms 内估计不准。
- 🟡 **开火角度误差 = 容差上限**（见 §6）—— 因为一进容差带就开火，实际误差约等于容差而非远小于它。这是 §6 那次修复针对的对象。
- 🟢 **转向速度封顶 180°/s**（`RotateTowards` 用 `NavMeshAgent.angularSpeed`，无更快转向旁路）。
- 🟢 **力度与方向必须用同一个点**：`TryFire()` 里瞄准点、射程判断、发射力度都用 `aimDistance`（预测点）—— 避免「方向朝预测点、力度按当前距离」的偏差（早期 bug，已修）。
- 🟢 **力度更激进**：开火用 `MaxForceWithoutOvershoot`，比旧「落地力度」快、飞行时间短 —— 好处是玩家更难反应、提前量更小；代价是命中窗口（炮弹在车身高度停留的时间）也更短。

### 飞过头顶的机制（重要）

`PassHeightFactor = 1.0` 下，炮弹在到达瞄准点**之前一直高于 1.7m**（弹道顶点在 d≈10m 处达 2.58m），**只在瞄准点那一个距离上降到 1.7m** —— 而 1.7m 恰好是坦克碰撞体的**顶面**。

所以**任何方向的提前量误差都会先表现为「从车顶掠过」**：

- **偏长**（弹着点超过玩家）→ 炮弹越过瞄准点后继续下降，仍能打进车身 ✓
- **偏短**（弹着点在玩家身后）→ 炮弹在玩家位置还高于车顶 → **飞过 + 落在身后** ✗

这就是为什么「提前量必须准」在这个 `PassHeightFactor` 下格外重要，也是 §4.4（消偏置）与 §6（按距离收紧容差）两处修复要解决的问题。

> d=20m 时的容错：偏短 **1.0m 以内仍能命中**（胶囊最低点 1.672m < 顶面 1.7m），超过 1.5m 才彻底飞过。所以 §4.4 修掉的 0.33–0.60m 偏置落在容错**边缘**，恰好解释了「有时中、常常不中」。
